<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.booking.boot.mapper.EnrollmentMapper">
  	<insert id="insertEnrollment">
  		insert into enrollment(enrollment_id, course_id, user_id, status, enrolled_at, instructor_id)
  		values (#{enrollment_id}, #{course_id}, #{user_id}, #{status}, #{enrolled_at}, #{instructor_id})
  	</insert>
  </mapper>